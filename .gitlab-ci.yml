include:
  - project: 'gnome/citemplates'
    file: 'flatpak/flatpak-ci-initiative-sdk-extensions.yml'

flatpak:
  image: 'registry.gitlab.gnome.org/gnome/gnome-runtime-images/rust_bundle:3.38'
  variables:
    BUNDLE: "identity-nightly.flatpak"
    MANIFEST_PATH: "build-aux/org.gnome.gitlab.YaLTeR.IdentityDevel.json"
    FLATPAK_MODULE: "identity"
    MESON_ARGS: "-Dprofile=development"
    APP_ID: "org.gnome.gitlab.YaLTeR.IdentityDevel"
    RUNTIME_REPO: "https://nightly.gnome.org/gnome-nightly.flatpakrepo"
  extends: '.flatpak'

# Configure and run rustfmt
# Exits and builds fails if on bad format
rustfmt:
  image: "rust:slim"
  script:
    - rustup component add rustfmt
    # Create blank versions of our configured files
    # so rustfmt does not yell about non-existent files or completely empty files
    - echo -e "" >> src/config.rs
    - echo -e "" >> src/static_resources.rs
    - rustc -Vv && cargo -Vv
    - cargo fmt --version
    - cargo fmt --all -- --color=always --check
